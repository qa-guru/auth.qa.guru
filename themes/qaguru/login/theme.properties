parent=keycloak.v2
import=common/keycloak

# Parent styles first (PatternFly + keycloak.v2), then the school overlay.
# Child `styles=` replaces the inherited list — do not drop css/styles.css.
styles=css/styles.css css/login.css

darkMode=true
